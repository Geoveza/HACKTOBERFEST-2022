import lib
import multithread from pool
import curl

ef connect(host='http://google.com'):
    try:
        urllib.urlopen(host)
        return True
    except:
        return False


def stana():
    for i in msg:
        sys.stdout.write(i)
        sys.stdout.flush()
        time.sleep(0.02)
